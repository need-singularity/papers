.DS_Store
.local/
*.pyc
__pycache__/

# TECS-L calc symlink
calc
# TECS-L .shared symlink
.shared
tecsrs

.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/hooks/

# Legacy scripts replaced by .hexa (kept on disk for P-002 launcher compatibility)
# scripts/ moved to tool/ in 2026-04-28 cycle (CLI cleanup, da47e40 design).
tool/sync_zenodo.py
tool/verify_dois.py
tool/infinite_growth.sh
tool/lib/growth_common.sh
tecs-l/P-002-figures.py
tecs-l/P-002-reproduction.py
upload_all_unpublished.sh
upload_n6_fusion.sh
upload_n6_neuro.sh
upload_pa38.sh
upload_zenodo.sh

# Phase 1 CLI — local runtime state / caches
.hook-commands/
CLAUDE.md.bak
state/cache/
state/*.tmp
*.credentials
*.credentials.*
# Never commit local-override creds — use secret repo instead:
#   papers publish calls `secret get zenodo.token` at runtime.

# .workspace machine-local SSOT (mk2 schema, NEVER github push) — added 2026-05-02
.workspace
.workspace.*
.workspace.lock

# scanner (ready-absorber) per-file digests — local only, never commit (may carry content_preview leaks)
.growth/absorbed/
